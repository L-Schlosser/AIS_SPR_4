# Progress Log
This file tracks learnings across iterations.

## Iteration 1 - Task 0.1: Create pyproject.toml
- Created pyproject.toml with all dependency groups (train, ocr, viz, dev)
- Ran `uv sync --group dev` successfully
- Files changed: pyproject.toml
- Learnings for future iterations:
  - uv resolved 113 packages, installed 26 for base + dev
  - onnxruntime 1.24.2 installed successfully
  - Python 3.12.11 used
  - `.venv` created automatically by uv
---

## Iteration 2 - Task 0.2: Create directory structure and __init__.py files
- Created all directories: data/samples/{3 types}, data/schemas, edge_model/{classification,extraction,inference}, ocr, api, mobile_app/src, scripts, tests/{unit,integration,e2e}, docs
- Created .gitkeep files in data dirs, scripts, docs
- Created __init__.py files in all Python packages with module docstrings
- Files changed: 18 new files across the directory structure
- Learnings for future iterations:
  - Use `edge_model` (underscore) not `edge-model` (hyphen) for Python packages
  - All imports verified: `import edge_model; import ocr; import api` works
  - ruff check passes with all new files
---

## Iteration 3 - Task 0.3: Create .gitignore
- Created .gitignore with all required patterns
- Files changed: .gitignore
- Learnings for future iterations:
  - pytest exit code 5 = no tests collected (expected when no test files exist yet)
  - ruff check passes on all existing files
  - git warnings about LF→CRLF are normal on Windows
---

## Iteration 5 - Task 1.1: Create Arztbesuchsbestätigung JSON schema
- Created `data/schemas/arztbesuchsbestaetigung.json` with Draft-07 schema
- All required fields: document_type, patient_name, doctor_name, facility_name, visit_date
- Optional fields: facility_address, visit_time, duration_minutes, confidence
- Files changed: data/schemas/arztbesuchsbestaetigung.json
- Learnings for future iterations:
  - jsonschema.Draft7Validator.check_schema() validates the schema structure itself
  - Schema uses `const` for document_type, `pattern` for dates/times, `additionalProperties: false`
  - No tests to run yet for Phase 1 schemas (tests come in Task 1.4+)
---

## Iteration 6 - Task 1.2: Create Reisekostenbeleg JSON schema
- Created `data/schemas/reisekostenbeleg.json` with Draft-07 schema
- Required fields: document_type, vendor_name, date, amount, currency
- Optional fields: vendor_address, vat_rate, vat_amount, category, description, receipt_number, confidence
- Uses `exclusiveMinimum: 0` for amount, `enum: ["EUR"]` for currency, category enum for expense types
- Files changed: data/schemas/reisekostenbeleg.json
- Learnings for future iterations:
  - Same pattern as arztbesuchsbestaetigung schema - const for doc type, pattern for dates
  - Austrian VAT rates (10%, 13%, 20%) referenced in PRD but schema allows 0-100 range
---

## Iteration 7 - Task 1.3: Create Lieferschein JSON schema
- Created `data/schemas/lieferschein.json` with Draft-07 schema
- Required fields: document_type, delivery_note_number, delivery_date, sender, recipient
- Nested objects for sender/recipient with required name field and optional address
- Items array with required description, quantity, unit per item
- Optional fields: order_number, total_weight, confidence
- Files changed: data/schemas/lieferschein.json
- Learnings for future iterations:
  - All 3 JSON schemas now complete in data/schemas/
  - Lieferschein schema is the most complex with nested objects and arrays
  - Next task (1.4) creates Pydantic models + first real tests
---

## Iteration 8 - Task 1.4: Create Pydantic models and DocumentType enum
- Created `api/models.py` with DocumentType enum, 3 document result models, SenderRecipient, DeliveryItem, ProcessingResult
- Created `tests/unit/test_models.py` with 28 tests covering valid/invalid data, missing fields, extra fields, enum values
- Files changed: api/models.py, tests/unit/test_models.py, PRD.md
- Learnings for future iterations:
  - Pydantic v2 uses `model_config = {"extra": "forbid"}` (not Config inner class)
  - Use `pattern` in Field() for string validation (dates, times, document_type constants)
  - ruff requires imports sorted: third-party (pytest, pydantic) before local (api.models) with blank line separator
  - All 28 tests pass in 0.27s
---

## Iteration 9 - Task 1.5: Create schema validation utility
- Created `edge_model/inference/validator.py` with SchemaValidator class
  - `validate(data, document_type)` returns (is_valid, error_messages)
  - `get_schema(document_type)` returns raw schema dict or raises ValueError
  - Loads schemas from data/schemas/ directory at init time
- Created `tests/unit/test_validator.py` with 18 tests covering:
  - Schema loading (all 3 types, empty dir, nonexistent dir, custom schema)
  - Valid data for all 3 document types including optional fields
  - Invalid data: missing fields, wrong types, extra fields, bad dates, bad amounts
  - Wrong document type handling
- Files changed: edge_model/inference/validator.py, tests/unit/test_validator.py, PRD.md, progress.txt
- Learnings for future iterations:
  - jsonschema.Draft7Validator.iter_errors() is better than validate() for collecting all errors
  - SchemaValidator will be used by the pipeline (Task 6.3) and service (Task 7.1)
  - Phase 1 is now complete - all schemas, Pydantic models, and validation utility done
---

## Iteration 10 - Task 2.1: Create document image generator
- Added `faker` to train dependency group via `uv add faker --group train`
- Created `scripts/generate_samples.py` with three generators:
  - `generate_arztbesuch()`: White canvas 800x1130, header "BESTÄTIGUNG ARZTBESUCH", German doctor/patient/facility fields
  - `generate_reisekosten()`: Header varies (RECHNUNG/QUITTUNG/BELEG), company info, amounts with MwSt, categories
  - `generate_lieferschein()`: Header "LIEFERSCHEIN", sender/recipient blocks, items table with qty/unit
- Augmentations applied to 50% of images: rotation ±3°, brightness ±15%, Gaussian noise, optional blur
- Each image saved as PNG with companion `_label.json` matching JSON schemas
- Files changed: scripts/generate_samples.py, pyproject.toml (faker added), uv.lock
- Learnings for future iterations:
  - Pillow font loading: try multiple font names (arial, DejaVuSans, LiberationSans) with fallback to default
  - Generated labels validated against all 3 JSON schemas with 0 errors
  - ruff line-length 120: break long lines by extracting variables or using multi-line args
  - Images are gitignored (data/samples/**/*.png) but schemas are not
  - Phase 2 started - next is Task 2.2 (NER text sample generator)
---

## Iteration 11 - Task 2.2: Create NER text sample generator
- Created `scripts/generate_text_samples.py` with BIO-tagged text generators for all 3 doc types
- Each generator has 4 layout variants (standard, compact, verbose, reordered) to simulate varied OCR output
- 30% chance of trailing noise tokens (stamps, page numbers, etc.)
- Output: JSONL files with 80/20 train/val split per document type
- Helper functions: `_tokenize_and_tag()` for BIO tagging, `_add_o_tokens()` for O-tagged words
- Files changed: scripts/generate_text_samples.py, PRD.md
- Learnings for future iterations:
  - ruff catches f-strings without placeholders (F541) - use plain strings instead
  - Faker de_DE generates realistic German names/addresses/companies
  - Token/tag alignment is critical - always assert `len(tokens) == len(ner_tags)`
  - Generated JSONL files are gitignored (data/samples/**/*.jsonl)
  - Next task (2.3) creates tests for both image and NER data generation
---

## Iteration 12 - Task 2.3: Create data loading and verification tests
- Created `tests/conftest.py` with shared fixtures:
  - sample_arztbesuch_image, sample_reisekosten_image, sample_lieferschein_image (generate 1 image in tmp dir)
  - sample_ner_data (generates 5 NER samples per doc type in memory)
  - schemas_dir (path to data/schemas/)
  - all_schemas (loads all 3 schemas as dicts)
- Created `tests/unit/test_data_generation.py` with 27 tests across 7 test classes:
  - TestLabelSchemaValidation: labels match JSON schemas (including file-based test)
  - TestNERTokenTagAlignment: tokens/tags same length at small and large scale
  - TestExpectedBIOTags: all expected BIO tag types appear, valid BIO format
  - TestAugmentation: pixel changes, dimension preservation, pixel range
  - TestGermanLocale: dates, Dr. prefix, Praxis prefix, LS- prefix, EUR currency, Austrian VAT rates
  - TestNERDocumentType: correct document_type field per generator
  - TestImageGeneration: 800x1130 RGB dimensions for all 3 doc types
- Files changed: tests/conftest.py, tests/unit/test_data_generation.py, PRD.md, progress.txt
- Learnings for future iterations:
  - ruff auto-fix (`--fix`) handles import sorting well — use it when I001 errors persist
  - Don't import pytest in test files unless directly used (fixtures come from conftest)
  - Phase 2 is now complete — all synthetic data generation and tests done
  - Next is Phase 3: Document Classification (Task 3.1)
---

## Iteration 13 - Task 3.1: Create classification dataset and config
- Created `edge_model/classification/config.py` with ClassificationConfig dataclass
  - Fields: image_size=224, num_classes=3, batch_size=16, lr_frozen/unfrozen, epochs, model_name, class_names
- Created `edge_model/classification/dataset.py` with:
  - DocumentDataset: loads images from subdirectories, directory name = label
  - get_transforms(): training (RandomResizedCrop, flip, jitter) and eval (Resize, CenterCrop) with ImageNet normalization
  - create_dataloaders(): stratified train/val split using StratifiedShuffleSplit
  - _SampleListDataset: lightweight wrapper for split subsets with transforms
- Created `tests/unit/test_classifier_dataset.py` with 16 tests in 3 classes
- Files changed: edge_model/classification/config.py, edge_model/classification/dataset.py, tests/unit/test_classifier_dataset.py, PRD.md
- Learnings for future iterations:
  - Don't subclass torch Subset for transform application — Subset.__getitem__ may bypass overrides. Use a standalone Dataset wrapper instead.
  - StratifiedShuffleSplit from sklearn works well for balanced class splits
  - Train group dependencies (torch, torchvision, sklearn) already synced from previous iterations
  - 89 total tests pass across the project in ~9s
---

## Iteration 14 - Task 3.2: Create classification training script
- Created `edge_model/classification/train.py` with two-phase transfer learning:
  - `create_model()`: loads timm EfficientNet-Lite0, replaces classifier head
  - `freeze_backbone()` / `unfreeze_all()`: toggle backbone parameter training
  - `train_epoch()`: single training epoch with loss + accuracy tracking
  - `validate_epoch()`: eval with per-class precision/recall metrics
  - `train()`: full pipeline — Phase 1 (frozen backbone) → Phase 2 (full fine-tuning), saves best model + metrics.json
  - CLI with argparse for data-dir and output-dir
- Created `tests/unit/test_classifier_train.py` with 10 tests:
  - TestCreateModel: output shape, num_classes, nn.Module check
  - TestFreezeBackbone: frozen params, trainable count, unfreeze verification
  - TestTrainEpoch: train_epoch runs, validate_epoch runs, per-class metrics structure
- Files changed: edge_model/classification/train.py, tests/unit/test_classifier_train.py, PRD.md
- Learnings for future iterations:
  - ruff E741: avoid variable name `l` (ambiguous) — use `lbl` instead
  - timm model downloaded from HuggingFace Hub on first use (~16MB for EfficientNet-Lite0)
  - 99 total tests pass across the project in ~12s
---

## Iteration 15 - Task 3.3: Create ONNX export for classifier
- Created `edge_model/classification/export_onnx.py` with:
  - `export_to_onnx()`: loads PyTorch model, exports to ONNX format with dynamic batch axes
  - `quantize_model()`: INT8 dynamic quantization via onnxruntime
  - `verify_onnx()`: runs ONNX inference with random or image input, asserts output shape
  - CLI entry point for export → quantize → verify pipeline
- Created `tests/unit/test_classifier_export.py` with 8 tests:
  - TestExportToOnnx: valid ONNX file, correct I/O names
  - TestQuantizeModel: quantized file exists and is valid
  - TestVerifyOnnx: correct output shape, random input, sample image, batch inference
- Added `onnxscript` to train dependency group (required by torch>=2.9 for ONNX export)
- Files changed: edge_model/classification/export_onnx.py, tests/unit/test_classifier_export.py, pyproject.toml, uv.lock, PRD.md
- Learnings for future iterations:
  - PyTorch 2.9+ uses dynamo-based ONNX export by default — must use `dynamo=False` for TorchScript-based export compatible with onnxruntime quantization
  - `onnxscript` package required as a dependency for torch.onnx.export to work at all
  - Use a shared `_export_tiny_model()` helper in tests to avoid repeating export boilerplate
  - 107 total tests pass across the project in ~13s
---

## Iteration 16 - Task 3.4: Create classification validation script
- Created `edge_model/classification/validate.py` with:
  - `compute_metrics()`: computes accuracy, per-class precision/recall/F1, confusion matrix from predictions+labels
  - `validate_onnx_model()`: loads ONNX model, runs inference on images from subdirectories, computes and prints metrics, saves to JSON
  - `_preprocess_image()`: shared image preprocessing (resize, normalize, HWC→CHW)
  - CLI with argparse for model-path and data-dir
- Created `tests/unit/test_classifier_validate.py` with 10 tests:
  - Perfect, all-wrong, and partial accuracy scenarios
  - Confusion matrix shape (3,3), values, row sums matching support
  - Precision/recall/F1 with known values
  - Empty inputs, support per class, all expected keys present
- Files changed: edge_model/classification/validate.py, tests/unit/test_classifier_validate.py, PRD.md
- Learnings for future iterations:
  - ruff catches f-strings without placeholders (F541) — same pattern as before, use plain strings
  - `compute_metrics()` is a pure function testable without ONNX — good pattern for separating logic from I/O
  - Phase 3 (Document Classification) is now complete
  - 117 total tests pass across the project in ~13s
---

## Iteration 17 - Task 4.1: Create OCR engine
- Created `ocr/engine.py` with OCREngine class wrapping RapidOCR:
  - `extract_text(image)` runs OCR and returns structured OCRResult with text, regions, processing_time_ms
  - `extract_text_from_file(file_path)` loads image then runs OCR
  - Converts RapidOCR polygon points to bounding box (x_min, y_min, x_max, y_max)
- Created `ocr/preprocessing.py` with image preprocessing:
  - `preprocess_for_ocr()`: grayscale conversion, sharpening, adaptive thresholding
  - `_adaptive_threshold()`: local mean-based adaptive threshold using PIL BoxBlur
- Created `ocr/postprocessing.py` with text region utilities:
  - `sort_regions_by_position()`: groups regions into lines by y-coordinate tolerance, sorts left-to-right within lines
  - `merge_text()`: combines sorted regions with spaces (same line) or newlines (different lines)
- Created `tests/unit/test_ocr.py` with 26 tests across 5 test classes:
  - TestPreprocessing (6): color/grayscale conversion, binary output, dimensions, dtype
  - TestSortRegionsByPosition (5): empty, single, top-to-bottom, left-to-right, multiline
  - TestMergeText (5): empty, single, same-line, different lines, readable output
  - TestOCREngine (7): init, extract with mock, empty results, multiple regions, file loading, bbox conversion, timing
  - TestDataClasses (3): TextRegion, OCRResult defaults and with regions
- Files changed: ocr/engine.py, ocr/preprocessing.py, ocr/postprocessing.py, tests/unit/test_ocr.py, PRD.md
- Learnings for future iterations:
  - `uv sync --group ocr` uninstalls train group packages — always sync all needed groups: `uv sync --group dev --group ocr --group train`
  - RapidOCR returns tuple of (detections_list, elapsed_time) — detections are [polygon_points, text, confidence]
  - ruff N814: don't alias CamelCase imports as _CONSTANT (e.g., `ImageFilter as _IF`)
  - Mock OCREngine by using `__new__` to bypass __init__, then set `_ocr` attribute to MagicMock
  - 143 total tests pass across the project in ~22s
---

## Iteration 18 - Task 4.2: Create OCR integration test
- Created `tests/integration/test_ocr_engine.py` with 5 integration tests:
  - test_extract_known_text: generates "Hello World 123" image, asserts OCR finds "hello" and "123"
  - test_processing_time_is_measured: verifies positive processing time
  - test_regions_have_bounding_boxes: validates bbox coordinates and confidence
  - test_extract_text_from_file: tests file-based OCR input
  - test_empty_image_returns_empty_text: blank image returns minimal regions
- All tests marked with @pytest.mark.integration
- Files changed: tests/integration/test_ocr_engine.py, PRD.md, progress.txt
- Learnings for future iterations:
  - RapidOCR works well on clean synthetic images — reliably extracts "Hello World 123"
  - Font fallback (arial → DejaVuSans → default) needed for cross-platform compatibility
  - OCR integration tests take ~26s due to model loading — significantly slower than unit tests
  - 148 total tests pass across the project in ~29s
  - Phase 4 (OCR Module) is now complete — next is Phase 5: Field Extraction (NER)
---

## Iteration 19 - Task 5.1: Create NER label definitions
- Created `edge_model/extraction/labels.py` with:
  - ARZTBESUCH_LABELS (14 tags), REISEKOSTEN_LABELS (15 tags), LIEFERSCHEIN_LABELS (17 tags)
  - LABEL_SETS dict mapping document_type strings to label lists
  - get_label2id() and get_id2label() helper functions
- Created `tests/unit/test_labels.py` with 30 tests across 5 classes:
  - TestLabelListsStartWithO: all lists start with "O"
  - TestBIOTagPairing: I- tags have matching B- tags, specific paired tags verified
  - TestLabel2IdAndId2Label: inverse mapping, length, O=0
  - TestLabelSets: all 3 doc types present, correct references
  - TestLabelUniqueness: no duplicates, valid BIO format
- Files changed: edge_model/extraction/labels.py, tests/unit/test_labels.py, PRD.md
- Learnings for future iterations:
  - ruff E741 still catches `l` variable name — use `lbl` instead
  - Phase 5 started — next is Task 5.2 (NER dataset and config)
---

## Iteration 20 - Task 5.2: Create NER dataset and config
- Created `edge_model/extraction/config.py` with ExtractionConfig dataclass:
  - model_name="distilbert-base-german-cased", max_length=256, batch_size=16, lr=5e-5, epochs=20, weight_decay=0.01
- Created `edge_model/extraction/dataset.py` with:
  - `load_ner_data()`: loads JSONL files with validation (token/tag length, required keys)
  - `NERDataset`: tokenizes with DistilBertTokenizerFast, handles subword alignment (-100 for non-first subwords, [CLS], [SEP], [PAD])
  - `create_ner_dataloaders()`: factory for train/val DataLoaders
- Created `tests/unit/test_ner_dataset.py` with 18 tests across 4 classes:
  - TestLoadNERData (7): valid JSONL, content preservation, missing keys, length mismatch, empty, blank lines
  - TestNERDataset (8): length, keys, shapes, special tokens -100, padding -100, subword alignment, known mapping, dtypes
  - TestBatchCollation (2): batch and single-item batching
  - TestCreateNERDataloaders (1): factory returns correct loaders with expected batch sizes
- Files changed: edge_model/extraction/config.py, edge_model/extraction/dataset.py, tests/unit/test_ner_dataset.py, PRD.md
- Learnings for future iterations:
  - DistilBertTokenizerFast `is_split_into_words=True` enables pre-tokenized input with word_ids() for alignment
  - Subword alignment: word_ids() returns None for special tokens, same ID for subwords of same word
  - distilbert-base-german-cased downloaded from HuggingFace Hub (~250MB) on first use
  - 196 total tests pass across the project in ~47s
---

## Iteration 21 - Task 5.3: Create NER training script
- Created `edge_model/extraction/train.py` with:
  - `compute_metrics_fn()`: returns a closure for HuggingFace Trainer, seqeval-style evaluation ignoring -100 and "O" tags
  - `compute_per_entity_f1()`: per-entity-type precision/recall/F1 computation
  - `train_ner_model()`: full training pipeline using HuggingFace Trainer with TrainingArguments
  - Saves model, tokenizer, and training_metrics.json to output_dir
  - CLI with argparse for document-type, train-path, val-path, output-dir
- Created `tests/unit/test_ner_train.py` with 12 tests across 4 classes:
  - TestModelCreation (3): correct num_labels, output shape, all doc types have labels
  - TestComputeMetrics (5): perfect/wrong/mixed predictions, -100 ignored, expected keys
  - TestPerEntityF1 (2): per-entity metrics, empty predictions
  - TestTrainingRun (2): 1-epoch training on tiny data, invalid doc type raises ValueError
- Added `accelerate` to train dependency group (required by HuggingFace Trainer)
- Files changed: edge_model/extraction/train.py, tests/unit/test_ner_train.py, pyproject.toml, uv.lock, PRD.md
- Learnings for future iterations:
  - HuggingFace Trainer requires `accelerate>=1.1.0` — must be in train dependency group
  - `remove_unused_columns=False` is critical in TrainingArguments when dataset returns custom dict keys
  - compute_metrics receives (logits, label_ids) tuple — use np.argmax on logits for predictions
  - 208 total tests pass across the project in ~54s
---

## Iteration 22 - Task 5.4: Create NER postprocessing
- Created `edge_model/extraction/postprocess.py` with:
  - `bio_tags_to_fields()`: merges B-/I- tagged tokens into field dict, handles subword ## cleanup, duplicate B- tags (keeps first), I- without B- ignored
  - `postprocess_arztbesuch()`: maps PATIENT→patient_name, DOCTOR→doctor_name, etc.; parses dates (DD.MM.YYYY, DD/MM/YYYY) to ISO, times (HH.MM, "Uhr") to HH:MM, duration to int
  - `postprocess_reisekosten()`: maps VENDOR→vendor_name, etc.; parses German amounts (comma decimal, thousands dots), defaults currency to EUR, normalizes category to lowercase enum
  - `postprocess_lieferschein()`: builds nested sender/recipient objects, groups ITEM_DESC/QTY/UNIT into items array, maps DELNR→delivery_note_number
  - `POSTPROCESSORS` dict mapping doc_type to its postprocessor function
- Created `tests/unit/test_postprocess.py` with 42 tests across 6 classes:
  - TestBioTagsToFields (12): simple, multi-token, subwords, O tags, empty, duplicates, mismatched lengths
  - TestPostprocessArztbesuch (8): full fields, date formats, time formats, duration parsing, schema validation
  - TestPostprocessReisekosten (7): full fields, euro sign, German thousands, default currency, categories, schema validation
  - TestPostprocessLieferschein (7): full fields, sender/recipient, items, defaults, schema validation
  - TestPostprocessors (3): all types present, callable, accept empty dict
  - TestEdgeCases (5): end-to-end BIO→postprocess for each type, slash dates, unparseable amounts
- Files changed: edge_model/extraction/postprocess.py, tests/unit/test_postprocess.py, PRD.md
- Learnings for future iterations:
  - German number format: comma=decimal, dot=thousands (1.234,56 → 1234.56)
  - Schema validation in tests catches postprocessor output mismatches early
  - bio_tags_to_fields is reusable across all doc types — postprocessors just reshape the raw dict
  - 250 total tests pass across the project in ~52s
---

## Iteration 23 - Task 5.5: Create NER ONNX export
- Created `edge_model/extraction/export_onnx.py` with:
  - `export_ner_to_onnx()`: uses optimum ORTModelForTokenClassification.from_pretrained(export=True), copies tokenizer
  - `quantize_ner_model()`: INT8 dynamic quantization, copies non-ONNX files (config, tokenizer) to output
  - `verify_ner_onnx()`: loads ONNX model + tokenizer, tokenizes sample text, runs inference, prints predictions
  - CLI entry point for export → quantize → verify pipeline for all 3 doc types
- Created `tests/unit/test_ner_export.py` with 9 tests across 3 classes:
  - TestExportNerToOnnx (3): ONNX files created, tokenizer copied, inference works
  - TestQuantizeNerModel (3): quantized model smaller, inference works, missing ONNX raises error
  - TestVerifyNerOnnx (3): logits shape, longer text, missing ONNX raises error
- Updated `pyproject.toml`: changed `optimum>=1.17.0` to `optimum[onnxruntime]>=1.17.0` (onnxruntime extra required for ORTModel)
- Files changed: edge_model/extraction/export_onnx.py, tests/unit/test_ner_export.py, pyproject.toml, uv.lock, PRD.md
- Learnings for future iterations:
  - `optimum[onnxruntime]` extra is REQUIRED — plain `optimum` doesn't include `optimum.onnxruntime` module
  - When creating tiny test models, use `tokenizer.vocab_size` from the actual tokenizer to avoid OOB embedding errors
  - optimum exports produce a directory (not single file) with model.onnx + config files
  - Phase 5 (Field Extraction NER) is now complete — all labels, dataset, training, postprocessing, ONNX export done
  - 259 total tests pass across the project in ~64s
---

## Iteration 24 - Task 6.1: Create classifier inference wrapper
- Created `edge_model/inference/preprocessor.py` with ImagePreprocessor class:
  - `prepare_for_classification()`: resize, normalize (ImageNet stats), HWC→CHW, add batch dim
  - `load_image()`: loads image file and returns RGB numpy array
- Created `edge_model/inference/classifier_inference.py` with ClassifierInference class:
  - `predict()`: preprocesses image, runs ONNX inference, applies softmax, returns (class_name, confidence)
  - `predict_batch()`: concatenates preprocessed images, runs batch inference
  - `_softmax()`: numerically stable softmax utility
- Created `tests/unit/test_classifier_inference.py` with 19 tests across 3 classes:
  - TestImagePreprocessor (8): shape, custom size, dtype, normalization range, zero/white image, load image, grayscale conversion
  - TestClassifierInference (8): predict format, class selection, confidence range, batch empty/multiple, input name verification
  - TestSoftmax (3): sum to one, uniform, large values stability
- Files changed: edge_model/inference/preprocessor.py, edge_model/inference/classifier_inference.py, tests/unit/test_classifier_inference.py, PRD.md
- Learnings for future iterations:
  - Reuse ImageNet mean/std constants from preprocessor (IMAGENET_MEAN, IMAGENET_STD) in tests
  - Mock ONNX session with `patch("...ort.InferenceSession")` to avoid needing real model files
  - Phase 6 started — next is Task 6.2 (extractor inference wrapper)
  - 278 total tests pass across the project in ~65s
---

## Iteration 25 - Task 6.2: Create extractor inference wrapper
- Created `edge_model/inference/extractor_inference.py` with ExtractorInference class:
  - `extract()`: tokenizes text, runs ONNX inference, converts tag IDs to BIO tags (skipping special tokens via offset_mapping), calls bio_tags_to_fields
  - `extract_and_postprocess()`: calls extract() then applies doc-type-specific postprocessor from POSTPROCESSORS dict
  - Uses offset_mapping from tokenizer to identify and skip special tokens ([CLS], [SEP], [PAD])
- Created `tests/unit/test_extractor_inference.py` with 14 tests across 3 classes:
  - TestExtractorInferenceTokenization (3): output shapes, dtype, offset_mapping availability
  - TestExtractorInference (6): returns dict, all-O empty, session keys/shapes/dtypes, known B-PATIENT tags produce field
  - TestExtractAndPostprocess (5): returns dict, adds document_type, unknown type raises, all 3 types work, reisekosten default EUR
- Files changed: edge_model/inference/extractor_inference.py, tests/unit/test_extractor_inference.py, PRD.md
- Learnings for future iterations:
  - offset_mapping (0,0) identifies special tokens — more reliable than checking token strings
  - Mock ONNX session pattern: patch ort.InferenceSession in __init__, then replace _session attribute
  - 292 total tests pass across the project in ~78s
  - Next is Task 6.3 (main pipeline orchestrator)
---
