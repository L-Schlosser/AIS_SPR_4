# Run app (hot reload enabled)
flutter run

# Run in verbose mode (debug issues)
flutter run -v

# Run on specific device
flutter run -d <device_id>

# Build for production (Android)
flutter build apk

# Build for production (iOS)
flutter build ios

# Clean project (if things break)
flutter clean
flutter pub get
flutter run

# Check code quality
flutter analyze

# Format code
flutter format lib/

# Run tests
flutter test